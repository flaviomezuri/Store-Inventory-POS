# Store-Inventory-POS
Store Inventory + POS is a Java Swing desktop application that simulates a small store’s inventory and point-of-sale workflow. It includes multi-screen GUI modules for Products, Customers, Orders, and Reports, persists data locally to CSV files, and implements validation, sorting/search algorithms, and order status rules (Draft/Paid/Cancelled).
